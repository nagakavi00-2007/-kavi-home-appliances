# AGENTS.md

## Project Context

This is a user-owned application repository. Keep changes focused on the user's request and preserve the existing project conventions.

Start with `README.md` for local setup and project workflow.

## Key Files

- `src/`: frontend application source.
- `vite.config.js`: Vite config.
- `package.json`: dependency and script entry points.

## Working Notes

- Prefer the existing project patterns instead of introducing new framework-specific wrappers.
- Run the appropriate checks from `package.json` before finishing code changes.
- Keep changes focused and avoid unnecessary churn.
